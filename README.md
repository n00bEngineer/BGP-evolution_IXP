# BGP-evolution_IXP
This work presents a dynamic routing optimization system for IXPs, which introduces real-time latency metrics into the BGP decision-making process through automatic integration with the BIRD route server.. This solves an evolutionary issue not present in the standard BGP protocol.
